# hibikiclaude — Claude向けプロジェクト指示

このリポジトリには2つの独立したツールキットがある。ユーザーの発話から該当するキットとスキルを判断して従うこと。

## タスクルーティング

| ユーザーの発話 | 使うキット | 詳細 |
|---|---|---|
| 「レビューして」「レビューして修正まで」「修正だけして」「ファクトチェックして」「ファクトチェックして修正まで」 | 記事トンマナ＆SEOレビューツール | [README.md](README.md) を読み、`config/`・`input/`・`templates/` の内容に従って実行する |
| 「資料作成して」「資料作って」「資料を作成して」「無料分析レポート作って」「〇〇様のLLMOレポート」「診断レポート出して」「登録フェーズ」「〇〇様をツールに登録」「プロンプト洗い出し」 | LLMO無料分析キット | [【公開版】LLMO無料分析/CLAUDE.md](【公開版】LLMO無料分析/CLAUDE.md) を読み、その指示・タスクルーティング表に従って実行する。**このフォルダ配下の相対パス（`02_スキル・エージェント/`・`03_クライアントレポート/`など）は `【公開版】LLMO無料分析/` を起点として解決すること** |

判断に迷う場合（発話だけでは判断できない場合）はユーザーに確認する。

## 「資料作成して」について

これはLLMO無料分析キットの汎用トリガー。以下の流れで対応する：

1. `【公開版】LLMO無料分析/CLAUDE.md` を読み込む
2. `【公開版】LLMO無料分析/02_スキル・エージェント/llmo-free-report/SKILL.md`（アウトプットフェーズのスキル本体）を読み込み、記載のSTEP 1〜5に従う
3. 発話にクライアント名が含まれていなければ、SKILLのSTEP 1でメンバーに確認する
4. 対象クライアントがまだツールに未登録・計測データが無い場合は、SKILLの停止ルール（STEP 1.5）に従い、`llmo-registration/SKILL.md`（登録フェーズ）へ誘導する

## Claude in Chrome（claude.aiの通常チャット）から使う場合

このCLAUDE.mdは、Claude Codeがこのリポジトリをプロジェクトとして開いたセッションでのみ自動的に読み込まれる。
Claude in Chrome（claude.aiの通常チャットからブラウザ操作する機能）はこのリポジトリを読み込まないため、
「資料作成して」等と言っても本ファイルのルーティングは効かない。
Claude in Chromeから使いたい場合は、[standalone-skills/](standalone-skills/README.md) にある
claude.ai Skills版（アカウントに直接登録するSkill）を使うこと。

## 各キットの独立性

- 記事レビューキット（ルート直下の `config/`・`input/`・`output/`・`templates/`）と LLMO無料分析キット（`【公開版】LLMO無料分析/`）は互いに依存しない。それぞれのファイルパス・ルールは自分のキット配下でのみ解決する
- LLMO無料分析キット内の `01_マニュアル/`・`02_スキル・エージェント/`・`04_共通素材/エンジン/lib/`・`.claude/` などは共有エージェント資産（[.claude/hooks/README.md](【公開版】LLMO無料分析/.claude/hooks/README.md) 参照）。オーナー以外からの書き換えはフックでブロックされる。編集したい場合はファイルを書き換えず、変更案を提示してオーナーに依頼する
